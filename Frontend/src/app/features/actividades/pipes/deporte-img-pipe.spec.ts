import { DeporteImgPipe } from './deporte-img-pipe';

describe('DeporteImgPipe', () => {
  it('create an instance', () => {
    const pipe = new DeporteImgPipe();
    expect(pipe).toBeTruthy();
  });
});

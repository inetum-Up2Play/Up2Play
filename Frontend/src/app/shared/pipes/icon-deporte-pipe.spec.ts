import { IconDeportePipe } from './icon-deporte-pipe';

describe('IconDeportePipePipe', () => {
  it('create an instance', () => {
    const pipe = new IconDeportePipe();
    expect(pipe).toBeTruthy();
  });
});
